import 'package:flutter/material.dart';

class PageMeetingEveningAdd extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text('新增夕会记录'),
        ),
      body: Center(
        child: new Text('新增夕会记录'),
      )
    );
  }
}